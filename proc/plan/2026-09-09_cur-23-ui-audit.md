> 기록 범위: 아래는 허브 UI·확장 문제 데이터가 함께 있던 원래 작업 폴더의 과거 검증 기록입니다. 이 독립 dev PR의 포함 범위와 검증 결과는 `2026-09-09_solving-dev-pr.md`를 따릅니다.

# CUR-23 감사 도구 정합화

현재 상태 안내(2026-09-09): 이 문서는 단계별 검증 이력이다. 아래 In Progress/남은 작업 표기는 작성 당시 상태이며, 이후 결과는 `2026-09-09_cur-23-final-exceptions.md` 및 `2026-09-09_games-final-ui-corrections.md`를 따른다. CUR-23은 완료 처리됐다. 화면 감사의 학습 본문 검사 확대 결과는 `2026-09-09_code-doc-review.md`를 따른다.

목표: 표준 `bun run dev` / `http://localhost:3033`의 정상 게스트 진입 후 목표 URL·게임 표시·오류·정확한 4 viewport를 검사한다. 기존 bbox 제한과 실패 종료 계약을 유지하며, 보이지 않는 요소는 검사하지 않고 자연 스크롤 요소는 스크롤하여 접근성을 측정한다.

비목표: 생성/문항/게임 채점/5장 회차/사용자 데이터 변경, 기존 사용자 미커밋 변경 복원, 배포. CUR-23 전체 17종 예외 흐름 완료는 이번 도구 수정만으로 선언하지 않는다.

대상: `/Users/taegwonson/Desktop/curea/games/apps/games/scripts`와 감사 회귀 테스트. `feat/games-json-content` @ `6ee94b0f4c20ab26960205150828fa661f8b96d6`. 시작 시 미커밋 191개 파일 해시를 `/tmp/cur23-baseline.json`에 보존.

수용 기준:
- 숨김/접힌 details를 제외하지만 표시된 가로 초과, 스크롤 불가능한 잘림, 페이지 예외, HTTP/목표 URL/viewport 불일치는 실패.
- 스크롤 가능한 긴 목록은 각 항목을 실제로 스크롤한 뒤 동일한 경계 조건으로 확인. 단순 informational 강등으로 통과시키지 않는다.
- 320×568 / 390×844 / 768×1024 / 1280×800을 실제 측정·assert.
- 오류 발생 시 전체 summary.pass=false, exit 1. 기존 catch 후 PASS 결함을 회귀로 차단.
- 분리된 브라우저 HTML fixture는 감사 알고리즘의 양성/음성 대조군에만 사용한다. 실제 앱 감사는 mock/신원 주입 없이 랜딩·게스트 폼을 사용한다.
- 루트 lint/typecheck/test/build 및 허브 실제 감사/관련 브라우저 테스트를 실행하고 실패 경계를 기록한다.

진행: 기존 82건은 전부 세로 위치 판정. 동작 검사는 정적 좌표를 읽기 전에 자연 스크롤을 수행하도록 수정한다. 규칙/AGENTS/spec의 임계값·critical 의미를 완화하지 않는다.

## 구현·실패 경계와 검증 결과

- 감사 CLI가 정상 게스트 폼, 목표 URL/본문 조작요소, HTTP/pageerror, 실제 viewport를 검사한다. 예외는 JSON pass=false와 exit 1로 전파한다.
- `scripts/ui-audit.mjs`로 측정 코드를 분리했다. 접힌 details·숨김 요소는 제외하고, 스크롤 가능한 조작요소는 스크롤 후 경계를 검사한다. hidden/clip 및 문서 가로축을 프로그램으로 움직여 접근 불가 요소를 통과시키지 않는다.
- Red: 숨김/스크롤 오판, device viewport 덮어쓰기, 접속 예외 후 PASS를 회귀 테스트로 재현. hidden/clip 및 문서 가로 초과 대조군도 실패 확인 후 수정. 최종 감사 회귀 6개 통과. 합성 HTML은 측정 엔진 대조군이며 실제 앱 로그인 대체가 아니다.
- 실제 허브: 320×568 / 390×844 / 768×1024 / 1280×800 모두 critical=0, pageerror=0. 기존 82건 위치 오판 해결. 증거: `output/playwright/cur23-hub/audit.json`.
- 공식 17종 × 4 크기 첫 화면: 최초 15종 통과, 영어 빈칸·수학 퀴즈는 hover scale 1.01이 스크롤 영역에서 2~3px 잘리는 4건 확인. screenshot에서도 보기 테두리 잘림 확인. 공통 `BlankComponent.tsx`, `QuickQuizComponent.tsx`의 hover 확대 한 줄씩 제거했다. 선택/정답 색, tap 효과, 클릭·채점 로직 유지. 두 파일은 이번 작업 시작 시 미커밋 대상이 아니었다.
- 수정 두 게임 × 4 크기 재검사 모두 critical=0, pageerror=0. 나머지 15종은 재검사 없이 최초 통과 증거 사용. 최초 실패: `output/playwright/cur23-games/`; 수정 후: `output/playwright/cur23-fixed/`.
- 정상 게스트 기반 허브 관련 E2E 23개 재통과. 독립 검수 hub_review: 중요 회귀 없음(STATIC_INSPECTED). 본문 버튼 표시는 카드별 내용 검증의 대체가 아니므로 감사 주석의 과도한 주장을 제거했다.

변경 파일: `apps/games/scripts/capture-ui-audit.mjs`, 신규 `apps/games/scripts/ui-audit.mjs`, 신규 `apps/games/e2e/ui-audit.spec.js`, 위 두 공통 메커니즘 컴포넌트, 이 계획 문서. 기존 미커밋 191개 파일 SHA-256 일치 확인. 문제 생성·콘텐츠 변경 없음.

증거 수준: 실제 게스트/URL/첫 화면 스크롤 경계는 RUNTIME_VERIFIED, 허브 관련 23개는 E2E_VERIFIED. 미검증: CUR-23 전체 17종 정답·오답·공개·완료·재시도·중복 다음·이탈 callback·새로고침·모드·저장 실패·전체 키보드 매트릭스. 감사는 조작요소의 경계 검사이며 다른 요소의 덮임이나 모든 텍스트 잘림을 보장하지 않는다. CUR-23은 In Progress 유지한다.

복구: 이번 감사 스크립트 변경과 두 컴포넌트의 hover 한 줄만 역패치하고 신규 테스트/엔진을 함께 회수한다. 기존 미커밋 파일을 전체 checkout/reset하지 않는다. 로컬 변경 회수만 필요하며 외부 배포 없음.

최종 게이트: 루트 `bun run lint`(0 error, 기존 warning 1), `bun run typecheck`, `bun run test`(54 files / 530 tests), `bun run build` 모두 통과. `git diff --check` 통과. 빌드 후 표준 `bun run dev` 재시작. CUR-15/CUR-20은 허브 수용 기준 충족으로 Done, CUR-23은 위 미검증 범위를 남기고 In Progress.

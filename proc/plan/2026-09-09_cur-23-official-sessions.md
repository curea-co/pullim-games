> 포트 구분(2026-09-10): 아래 `localhost:3033`은 당시 `feat/games-json-content` 작업 폴더에서 실제 검증한 주소이며 과거 기록으로 보존합니다. **현재 PR #157·dev에서 재현할 때는 `bun run dev` 후 `http://localhost:3004`를 사용합니다.** 실행 포트는 해당 브랜치의 `apps/games/package.json`을 기준으로 확인합니다. 배포 도메인의 HTTPS 접속 포트와 로컬 개발 포트는 별개입니다.

> 기록 범위: 아래는 허브 UI·확장 문제 데이터가 함께 있던 원래 작업 폴더의 과거 검증 기록입니다. 이 독립 dev PR의 포함 범위와 검증 결과는 `2026-09-09_solving-dev-pr.md`를 따릅니다.

# CUR-23 공식 15종 회차 검증

현재 상태 안내(2026-09-09): 이 문서는 단계별 검증 이력이다. 아래 In Progress/남은 작업 표기는 작성 당시 상태이며, 이후 결과는 `2026-09-09_cur-23-final-exceptions.md` 및 `2026-09-09_games-final-ui-corrections.md`를 따른다. CUR-23은 완료 처리됐다. 화면 감사의 학습 본문 검사 확대 결과는 `2026-09-09_code-doc-review.md`를 따른다.

목표: 정상 게스트 화면에서 입력 게임 2종 외 15종의 복습 5장 완주·재시도·새로고침·허브 복귀와 오답 공개를 검증한다.
비목표: 문제 생성, 카드 데이터, 계정 계약, 배포 변경. 기존 미커밋 작업을 보존한다.
대상: games/apps/games/e2e/official-session-flow.spec.js, 직접 구현 게임 12개 component.tsx의 모드 선택 조건, 이 문서.
기준: feat/games-json-content @ 6ee94b0f4c20ab26960205150828fa661f8b96d6. 시작 시 dirty 206개 파일 해시를 /tmp/cur23-full-baseline.json에 기록했다.

수용 기준: 표준 bun run dev / http://localhost:3033의 랜딩 → 게스트 폼 → 게임 흐름을 사용한다. 목표 URL, 실제 카드와 진행도, 완료, 재시도, 복귀 및 pageerror를 검사한다. 신원·정답 상태를 주입하지 않는다. 실제 batch는 UI 조작의 답안으로 읽기만 한다.

## 발견한 제품 결함

학습 이력이 없는 게스트에게 직접 구현 12종의 복습 모드가 전체 10장으로 열렸다. `all.size > 0`일 때만 모드별 카드 선택기를 호출하기 때문이다. 신규 테스트에서 기대 1/5, 실제 1/10으로 실패했다(RUNTIME_VERIFIED).

해당 조건을 `all.size > 0 || mode === "review-queue"`로만 수정했다. 기존 5장 선택기를 처음 방문한 게스트에게도 적용하며 기본 모드와 채점 정책은 유지한다. 원본 12개는 /tmp/cur23-mode-originals.json에 보존했다. dirty 파일에서는 이번 조건 한 줄을 역변환한 후 시작 해시와 비교해 기존 내용 보존을 확인했다. 예상 밖 변경 0개.

## 테스트 정합화와 독립 검수

- 인수분해는 unit 텍스트를 표시하지 않아 후보 영역·진행도·독립 계산한 인수로 확인한다.
- 그래프·벡터 버튼명은 ‘확인’, 품사는 토큰 선택 → 품사 적용 순서다.
- 생물 카드명에 괄호가 있어 정규식 대신 정확한 accessible name으로 선택한다.
- 공개 안내는 시각 요소와 sr-only 요소에 중복되므로 게임 본문 영역에서 찾는다.
- 오답 중 다음 버튼이 없는 게임과 비활성 버튼을 표시하는 게임 모두 ‘활성 다음 버튼 0개’로 확인한다.
- 유전 게임의 모두 0 입력은 제출 불가가 정상이다. 오답 검증은 제출 가능한 0:1 비율을 입력한다.
- 독립 검수에서 객관식·영어 빈칸은 오답도 다음이 활성화되므로 성공 판정이 약하다는 P2를 수용했다. 성공 solve에 ‘정답이에요’ 피드백 확인을 추가했고 재검수에서 해소 확인을 받았다. 제품 12개는 원본 대비 조건 한 줄씩만 변경됐음을 검수자가 확인했다(STATIC_INSPECTED).

## 실행 증거와 범위

복습 5장 완주 → 재시도 후 첫 문항 정답/다음 → 새로고침 후 게임 표시 → 허브 복귀: 15종 통과. 최초 솔버 실행에서 13종 통과(/tmp/cur23-official-final.log), 품사·생물 및 강화한 객관식 2종은 후속 실행에서 통과(/tmp/cur23-official-exceptions.log). 하나의 실행에서 15개가 모두 통과한 것으로 계산하지 않는다.

수정 12종 × 4 viewport의 review-queue 첫 화면 감사: 모두 pass, overflow 0, pageerror 0. 근거는 output/playwright/cur23-review-mode/summary.json과 게임별 audit.json이다. 화면 크기는 320×568 / 390×844 / 768×1024 / 1280×800. 완주 E2E는 1280×1000에서 실행했으므로 네 크기 전체 완주를 검증한 것은 아니다.

오답 정책도 구분한다. 객관식·영어 빈칸은 한 번 선택한 뒤 정답 피드백을 제공한다. 나머지 13종은 1~4회에 다음 진행을 막고, 5회에는 공개 안내와 다음 활성화를 확인한다. 모두 이후 2/5 진입과 페이지 오류 배열을 검사한다.

명령은 앱 디렉터리의 `bun run test:e2e official-session-flow.spec.js --workers=2` 및 수정된 시나리오를 좁힌 `--grep`이다. 합성 로그인이나 대체 서버는 사용하지 않았다. 단위 테스트는 저장 mock 등을 포함하므로 실제 브라우저 검증과 구분한다.

## 남은 범위와 복구

CUR-23은 In Progress다. 중복 다음, 이탈 callback, 저장 실패, 타임어택 만료·깊이 회상 데이터가 있는 회차, 모든 상태의 네 화면 크기·키보드 검증, 신규 테스트 CI 연결이 남았다. 이번 복습 5장 완주는 기본 모드 전체 풀 완주·새로고침 진도 유지·회원 동기화를 보장하지 않는다.

복구는 제품 12개 조건 한 줄의 역패치와 신규 공식 회차 테스트 회수로 한정한다. 기존 dirty 파일 전체 checkout/reset은 금지한다. 외부 푸시·배포는 하지 않았다.

## 최종 검증

고유 브라우저 시나리오 30개 통과: 위 완주 15개와 오답 공개 15개. 오답 공개 최종 실행은 14개 pass(/tmp/cur23-reveal-final.log), 유전 입력 수정 후 해당 1개 pass(/tmp/cur23-genetics-final.log, exit0). 여러 실행에서 수정한 실패를 해소한 결과이며 한 실행의30pass는 아니다. 최초 실패와 수정 후 통과 로그를 모두 남긴다. 증거 수준 E2E_VERIFIED: 로컬 실제 게스트·화면 조작·정답/오답/회차 전환.

루트 lint 통과(기존 warning 1), typecheck 통과, unit 54 files/530 tests 통과. git diff --check 및 기존 변경 보존 재확인 통과. 제품 변경은 직접 구현12종의 조건 한 줄씩, 신규 파일은 공식 회차 테스트와 본 문서이다.

루트 build exit0 확인. 브라우저 검사 종료 후 dev 서버를 중지하고 빌드했으며, 표준 bun run dev를 다시 시작했다. 커밋·푸시·배포 없음.

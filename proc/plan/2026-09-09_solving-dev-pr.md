# 풀이 오류 독립 dev PR

목표: 허브 UI 커밋을 제외하고 풀이 오류·접근성·검증 도구 수정을 최신 dev에 적용한다.
기준: origin/dev dc5b37e, 원본 수정 커밋 01c062f. 기존 사용자 작업 폴더는 보존한다.

## 범위
- 타이머 deadline, 중복 저장, 재시도 상태, 첫 복습 5장 제한, 키보드 조작, 도해·한자 표시.
- 화면 잘림 감사 엔진과 독립 회귀 테스트, 현재 구현에 맞춘 게임 설명.
- jsdom 및 JSX 테스트 설정은 독립 실행 의존성으로 포함한다.
- 허브 UI·모드 진입 개편, 문제 JSON·콘텐츠 연결·manifest 변경은 제외한다.
- 별도 확장 JSON에 의존한 E2E 추가/변경은 제외하고 dev의 기존 E2E를 유지한다. 해당 전체 완주 검증은 이 PR에서 주장하지 않는다.
- 기존 2026-09-09 보고서들은 원래 혼합 작업 폴더의 역사 기록이며 이 PR 검증 증거와 구분한다.

## 검증
- 독립 dev 작업 폴더에서 lint, typecheck, build 통과. 단위 테스트 634개 통과.
- 재시도 테스트의 정답 3x 고정과 동일 글자 교환 오답 가정을 발견하여 현재 카드 기준으로 수정했다(Red 3개 → Green).
- playwright.audit.config.ts: 인증 주입 없이 감사 대조군과 실제 랜딩을 검사, 10개 통과.
- 도해 실제 게스트 진입 및 4 viewport 감사 통과. 나머지 화면은 PR 본문에 최종 결과 기록.
- 전체 게임 완주, SSO·실제 회원 DB, SVG 내부 글자 잘림은 미검증.

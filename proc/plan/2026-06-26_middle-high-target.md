# 타겟 확장 — 중등 → 중·고등 (고등 배제 해제)

**작성일**: 2026-06-26
**작성자**: 대표님 확정 + claude
**상태**: IN PROGRESS — 중·고등 타겟 확정, dev→검증→main 흐름
**근거**: 대표님 확정 (2026-06-26) — "pullim-games 대상은 중·고등학생". #125(중등 단독)가 고등을 *배제*한 게 과했음 → 고등 배제만 되돌리고 중등 추가분은 유지.

## 0. 배경 — 타겟 변천

| 시점 | 타겟 |
|---|---|
| 원래 spec | 고등 전과목 |
| #125 (2026-06-26 머지) | **중등 only** ← 고등 배제 과함 |
| **본 PR (대표님 확정)** | **중·고등** = #125 의 고등 배제만 revert, 중등 추가분 유지 |

**초등은 여전히 제외** — 풀림 주니어(별도 앱) 영역. 불변. 관련: [[project_middle_school_repositioning]].

## 1. 변경 (고등 배제만 되돌림)

- [x] **고등 게임 노출 복원**: `physics-vector`·`chemistry-balance` 매니페스트의 `stage:"high"` 보관 태그 제거 → `visibleGames`(stage≠high 필터)·`perGame` 자동으로 17 게임 전부 노출. `GameMeta.stage` 필드·`visibleGames` 헬퍼는 유지(현재 태깅 게임 0 → `visibleGames===games`), 향후 "학년별 게임 제공" 필터로 재활용 예정.
- [x] **GRADES** `중1~중3` → **`중1·중2·중3·고1·고2·고3`** (중·고, 초등 제외). 초등(초1~초6)만 `isGrade` 거부 → 마이그레이션 로직 그대로 유효. 게스트·회원가입 드롭다운 자동 반영.
- [x] **spec/02·10**: `중등` → `중·고등` 포지셔닝. spec/02 §2.5 V1 데모 단원은 `고1 인수분해`(실제 factorization 게임·seed 콘텐츠 = 고1)로 정정. spec/05 §5.6 동의모델(under-14 허용)은 그대로 — 중1 여전히 유효.
- [x] 테스트 정합: 무효 학년 리터럴 `고1` → `초5`(이제 초등만 범위 밖), `registry.test` 보관→전체노출로 갱신.

## 2. 유지 (#125 중등 추가분 — 그대로 유효)
- 회원가입+게스트 **학년 수집**(중·고 다 필요), grade 서버·클라 계약 end-to-end.
- **동의모델 통일**(under-14 honest consent — 중1).
- 신원 마이그레이션(getPlayer 보존 / createPlayer 클린슬레이트+비용가드 보존).

## 3. 흐름 / 비목표
- **dev → dev-games.pullim.ai 검증(대표님/G1 로그인 접속) → main 승격** (#125 가 main 직행한 것 정정 — 본 PR 은 dev 로).
- 보류(콘텐츠 확장 트랙): 21게임 학령 태깅(stage 전 게임)·학년별 게임 필터 UX·manage 밴드 정책. 초등 신설 없음.

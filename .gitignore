# dependencies — monorepo: root + apps/*/node_modules
node_modules/

# turborepo
.turbo/

# next.js — anchored 제거: apps/games/.next 등 중첩 빌드물도 무시
.next/
.next-dev/
out/
build/

# typescript
*.tsbuildinfo
next-env.d.ts

# misc
.DS_Store
*.pem

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env*.local

# vercel
.vercel

# local dev postgres volume (docker compose) — proc/plan/2026-05-29_auth-login-signup.md
/pgdata

# gstack data (user-machine specific, not project)
/.gstack

# playwright mcp test artifacts
/.playwright-mcp

# IDE
.idea
.vscode

# work artifacts (design inputs/outputs, not committed)
/input
/output

# playwright e2e artifacts (apps/games 중첩 포함)
playwright-report/
test-results/
**/playwright/.cache
.gstack/
# playwright storageState (런타임 생성, 커밋 불필요)
**/.playwright/

# claude agent worktrees (embedded repos, not tracked)
/.claude/worktrees/

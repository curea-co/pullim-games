// history-timeline placeholder — status='coming-soon' 이라 /games/[gameId] 라우트가
// notFound() 로 빠지므로 실제 호출되지 않는다. 빌드/정적 생성 통과를 위해서만 존재.

export default function HistoryTimelinePlaceholder() {
  return null;
}
